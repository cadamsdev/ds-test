{
  "name": "root",
  "private": true,
  "scripts": {
    "watch": "npx lerna watch -- npx lerna run build --scope=\\$LERNA_PACKAGE_NAME --include-dependents",
    "storybook": "npx lerna run storybook"
  },
  "workspaces": [
    "styles/*",
    "components/*",
    "apps/*"
  ],
  "devDependencies": {
    "lerna": "^6.4.1"
  }
}
